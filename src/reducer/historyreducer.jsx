const historyReducer = (historyState,historyAction) => {
    switch (historyAction.type) {
        case "":
            
            break;
    
        default:
            break;
    }
}
export {historyReducer}